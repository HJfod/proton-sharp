# Documentation for .ptd format

Stored in plaintext (will add gzip compression later)

Markdown:

`[symbol]{text|path}`

Symbols:

b - bold
i - italics
ul - underline
img - image

Escaping:

`\[some text]{some text}`